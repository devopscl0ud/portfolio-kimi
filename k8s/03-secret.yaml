apiVersion: v1
kind: Secret
metadata:
  name: portfolio-secrets
  namespace: portfolio
type: Opaque
stringData:
  # Add sensitive data here (e.g., API keys)
  # VITE_AI_API_KEY: "your_secret_api_key_here"
  # Encode as base64 if using 'data' instead of 'stringData'
data: {}
