apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portfolio-dev-route
  labels:
    env: preview
spec:
  parentRefs:
  - name: portfolio-gateway
    namespace: portfolio-prod
  hostnames:
  - "dev-PLACEHOLDER.portfolio.vividp.cloud"  # Updated by CI/CD
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: portfolio-service
      port: 80
