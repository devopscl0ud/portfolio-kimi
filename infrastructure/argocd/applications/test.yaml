apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: portfolio-test
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/devopscl0ud/portfolio-kimi.git
    targetRevision: test
    path: k8s-gitops/overlays/test
  destination:
    server: https://kubernetes.default.svc
    namespace: portfolio-test
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
